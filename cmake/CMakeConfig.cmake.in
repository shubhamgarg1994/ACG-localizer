
get_filename_component(ACGLOCALIZER_INSTALL_PREFIX ${CMAKE_CURRENT_LIST_FILE} PATH)
set(ACGLOCALIZER_INSTALL_PREFIX "${ACGLOCALIZER_INSTALL_PREFIX}/../..")

set(CMAKE_MODULE_PATH ${ACGLOCALIZER_INSTALL_PREFIX}/share/acglocalizer/cmake)

set(ACGLOCALIZER_FOUND TRUE)

find_package(FLANN REQUIRED)
find_package(ANN REQUIRED)
find_package(GMM REQUIRED)
find_package(LAPACK REQUIRED)
find_package(OpenMesh REQUIRED)

set(AGCLOCALIZER_INCLUDE_DIRS
  ${CMAKE_CURRENT_SOURCE_DIR}/src
  ${GMM_INCLUDE_DIR}
  ${OPENMESH_INCLUDE_DIR}
  ${FLANN_INCLUDE_DIR}
  ${ANN_INCLUDE_DIR}
  )

set(ACGLOCALIZER_LINK_DIRS
    ${ACGLOCALIZER_INSTALL_PREFIX}/lib/acglocalizer
    )

set(ACGLOCALIZER_LIBRARIES
  ${ANN_LIBRARIES}
  ${FLANN_LIBRARIES}
  )
